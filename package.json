{
  "name": "ruvyxa-monorepo",
  "version": "1.0.31",
  "private": true,
  "type": "module",
  "description": "A production-minded web framework built around clarity, speed, control, and predictable application delivery.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check && pnpm lint",
    "lint": "oxlint -c .oxlintrc.json packages templates examples scripts tests",
    "lint:fix": "oxlint -c .oxlintrc.json --fix packages templates examples scripts tests",
    "test": "cargo test --workspace && pnpm -r test",
    "prepare": "node scripts/setup-git-hooks.mjs",
    "check:cargo-lock": "node scripts/check-cargo-lock.mjs",
    "check:oxc-lockstep": "node scripts/check-oxc-lockstep.mjs",
    "format": "cargo fmt --all && prettier --write .",
    "format:check": "cargo fmt --all --check && prettier --check .",
    "format:staged": "node scripts/format-staged.mjs",
    "check:template-mirrors": "node scripts/check-template-mirrors.mjs --check",
    "check:unused": "knip",
    "release:validate": "pnpm lint && node scripts/validate-package-metadata.mjs && node scripts/validate-release-publish-plan.mjs && node scripts/check-oxc-lockstep.mjs && node scripts/check-doc-links.mjs && node scripts/check-template-mirrors.mjs --check && node scripts/sync-adapters.mjs && knip",
    "release:bump": "node scripts/bump-version.mjs",
    "pack:smoke": "node scripts/pack-smoke.mjs",
    "verify:reproducible": "node scripts/verify-reproducible.mjs",
    "test:full-flow": "pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/test-full-flow.ps1",
    "publish:dry-run": "pnpm -r --filter './packages/**' publish --access public --dry-run"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "knip": "6.32.2",
    "oxlint": "1.79.0",
    "postcss": "^8.5.26",
    "prettier": "3.9.6",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=24.19.0"
  }
}
