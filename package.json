{
  "name": "ruvyxa-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Rust-powered full-stack TypeScript framework MVP.",
  "license": "MIT",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "cargo test --workspace && pnpm -r test",
    "format": "cargo fmt --all && pnpm -r format"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  }
}
