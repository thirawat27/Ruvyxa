# Claude Instructions

Read `AGENTS.md` first and follow it as the source of truth.

This is a Ruvyxa app with file-based routing under `app/`, React 19, TypeScript, and Node.js 22 runtime expectations.

Before handing off changes that affect routes, imports, config, environment usage, or production behavior, run:

```bash
pnpm check
```

