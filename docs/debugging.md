# Debugging

Ruvyxa diagnostics are structured around five questions:

1. What happened?
2. Where did it happen?
3. Why did it happen?
4. How do you fix it?
5. Which routes or modules are affected?

The MVP includes diagnostics for missing app directories, invalid route segments, duplicate route paths, pages without default exports, SSR renderer failures, and API route execution failures.
