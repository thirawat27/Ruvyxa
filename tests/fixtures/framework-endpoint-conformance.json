{
  "description": "Framework-owned endpoints and which Ruvyxa host must serve each one.",
  "why": "Ruvyxa has two request hosts: the Axum server behind `ruvyxa dev`/`start`, and `createHandler` in packages/ruvyxa/runtime/serverless-handler.mjs, which every adapter's function artifact and the generated standalone server run. Endpoints were added to the Axum router by hand and ported to the handler one at a time, with nothing checking that the two agreed. `/__ruvyxa/image` was ported; `/__ruvyxa/action` was not, so every server action -- the crud template's forms, examples/demo/app/todos, and the markup `ruvyxa add` generates -- returned 404 on every deployed build while working locally. This table is the agreement. Add the endpoint here first; both languages replay it.",
  "fields": {
    "reserved": "Must appear in RESERVED_FRAMEWORK_ROUTES (crates/ruvyxa_dev_server/src/lib.rs) and RESERVED_FRAMEWORK_PATHS (packages/ruvyxa/runtime/plugin-http.mjs). A plugin that registers one of these paths panics axum, so both lists have to name the same set.",
    "native": "Served by the Axum host. `always` in every mode, `dev` only when config.watch is set.",
    "serverless": "How createHandler answers. `dispatch` means the path is handled before route matching and must never fall through to the generic route-miss 404. `static` means the build emits the bytes and a CDN or publish directory answers, so a function never sees it. `none` means the endpoint does not exist outside the native host.",
    "probe": "Request the Node conformance test sends when serverless is `dispatch`. Use `probes` instead when an endpoint answers more than one verb: a single probe checks one verb and says nothing about the others, which is how `POST /__ruvyxa/rsc` -- every server-function call on a deployed server-components page -- returned 405 with this table green.",
    "probes": "Several such requests, replayed one at a time. An endpoint that answers more than one verb lists each of them here rather than one in `probe`.",
    "requiredHeaders": "Request headers the endpoint refuses with 400 when absent, as name -> value. This is where a same-origin gate that is not CORS is written down. `/__ruvyxa/rsc` renders with the visitor's cookies on GET and runs a server function on POST, and its only cross-origin defence is a header a third-party page cannot set without a preflight nothing answers -- spelled independently in `framework_endpoints.rs`, `serverless-handler.mjs`, `rsc-client-runtime.mjs`, and `@ruvyxa/react`'s router. The dispatch probes send these, and a companion test drops them one at a time, so a host that stopped checking answers 404-or-200 instead of 400 and fails here."
  },
  "endpoints": [
    {
      "path": "/__ruvyxa/hmr",
      "reserved": true,
      "native": "dev",
      "serverless": "none",
      "note": "Hot-module-replacement WebSocket. Requires a persistent connection and a watcher; neither exists in a build artifact."
    },
    {
      "path": "/__ruvyxa/client",
      "reserved": true,
      "native": "always",
      "serverless": "static",
      "note": "Hydration bundles. The build writes content-hashed files under /__ruvyxa/client/, so the CDN or the standalone server's publish directory answers and the function is never invoked."
    },
    {
      "path": "/__ruvyxa/action",
      "reserved": true,
      "native": "always",
      "serverless": "dispatch",
      "probe": {
        "method": "POST"
      },
      "note": "Server actions. This is the endpoint whose absence from createHandler broke every deployed form."
    },
    {
      "path": "/__ruvyxa/flight",
      "reserved": true,
      "native": "always",
      "serverless": "dispatch",
      "probe": {
        "method": "GET",
        "query": "path=/&artifact=0000000000000000"
      },
      "note": "Public, version-bound Flight navigation payloads. Invalid or stale requests fail before ordinary route matching."
    },
    {
      "path": "/__ruvyxa/rsc",
      "reserved": true,
      "native": "always",
      "serverless": "dispatch",
      "probes": [
        {
          "method": "GET",
          "query": "path=/",
          "requiredHeaders": {
            "x-ruvyxa-rsc": "1"
          }
        },
        {
          "method": "POST",
          "query": "path=/",
          "requiredHeaders": {
            "x-ruvyxa-rsc": "1",
            "x-ruvyxa-action": "sample-module#action"
          }
        }
      ],
      "note": "React Server Components. `GET` renders a route's payload for a soft navigation; `POST` runs one of the server functions that route exposes and answers with the payload its return value encodes to. Unlike /__ruvyxa/flight both are renders and carry the visitor's headers, so neither is cacheable. The deployed function answered 501 to `GET` and 405 to `POST` until the generated route registry learned to render through the server-components pipeline and to call into an action bundle."
    },
    {
      "path": "/__ruvyxa/trace",
      "reserved": true,
      "native": "dev",
      "serverless": "none",
      "note": "Runtime trace inspector, gated on config.debug_traces."
    },
    {
      "path": "/__ruvyxa/devtools",
      "reserved": true,
      "native": "dev",
      "serverless": "none",
      "note": "Devtools dashboard."
    },
    {
      "path": "/__ruvyxa/devtools/data",
      "reserved": true,
      "native": "dev",
      "serverless": "none",
      "note": "Devtools snapshot feed."
    },
    {
      "path": "/__ruvyxa/image",
      "reserved": true,
      "native": "always",
      "serverless": "dispatch",
      "probe": {
        "method": "GET",
        "query": "src=/a.png&w=64&q=80"
      },
      "note": "On-demand image optimization. Adapters that cannot optimize pass no optimizer and it answers 404, but the path is still claimed before routing."
    },
    {
      "path": "/__ruvyxa/hydration-loader.js",
      "reserved": true,
      "native": "always",
      "serverless": "none",
      "note": "Deferred-hydration loader for `idle` and `visible` routes. `ruvyxa build` writes the same source to a content-hashed /__ruvyxa/client/hydration-<hash>.js instead, so a deployed build never asks for this path. The axum router registers it unconditionally, which is what makes it reserved: it was registered and not listed, so a plugin could take it and panic the router at startup."
    },
    {
      "path": "/__ruvyxa/client/route-manifest.json",
      "reserved": true,
      "native": "always",
      "serverless": "static",
      "note": "Client route table, fetched by @ruvyxa/react's router. A build publishes the file under /__ruvyxa/client/ and a CDN or publish directory answers; the dev server has no such file and synthesizes an equivalent from the live manifest. Registered unconditionally on the axum router, so it is reserved for the same reason as the loader above."
    },
    {
      "path": "/__ruvyxa/client/vendor",
      "reserved": true,
      "native": "always",
      "serverless": "none",
      "note": "React and its family, compiled once and imported by URL from every bundle this host compiles on demand. A build gives those routes a shared chunk instead, so a deployed function never emits these URLs and never serves them."
    }
  ],
  "capabilities": [
    {
      "id": "actions",
      "nativeHost": true,
      "buildArtifact": true,
      "note": "Discovered from action.ts beside a page route by action_file_for in Rust and actionFileFor in adapter-runner.mjs, which must agree on the .ts-before-.js order."
    },
    {
      "id": "pluginHttp",
      "nativeHost": true,
      "buildArtifact": true,
      "note": "http.onRequest / http.onResponse / http.route, including all of @ruvyxa/auth. The Rust host spawns plugin-runtime.mjs; a function bundle compiles the registry in through adapter-runner.mjs. Both build it with createPluginRegistry."
    },
    {
      "id": "realtime@1",
      "nativeHost": true,
      "buildArtifact": false,
      "note": "Needs a socket upgrade. `ruvyxa build` warns RUV2205 rather than failing, because a deployment without the realtime endpoint is still a valid deployment."
    },
    {
      "id": "presence@1",
      "nativeHost": true,
      "buildArtifact": false,
      "note": "Same constraint as realtime@1."
    },
    {
      "id": "images@1",
      "nativeHost": true,
      "buildArtifact": false,
      "note": "config.image.onDemand. /__ruvyxa/image resizes through the Rust image pipeline, which only the Axum host has: createHandler takes an optimizeImage option and no adapter passes one, so a build artifact answers 404 there. Recorded here as well as in the endpoint row above, because the endpoint row says what the host does and this says what a project that enabled the option gets -- and the failure is silent, since a browser falls back to `src` and only pays a full-size download."
    }
  ]
}
